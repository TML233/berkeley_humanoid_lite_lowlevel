# 初始配置
lowlevel项目拉到本地之后，需要配置。
```bash
# 同步uv环境
uv sync
# 安装项目里指定的包
uv pip install -r requirements.txt

# 安装项目里缺失的包：torch和omegaconf
uv pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
uv pip install omegaconf
```